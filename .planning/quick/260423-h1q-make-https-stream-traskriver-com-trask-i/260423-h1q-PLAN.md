---
phase: quick-260423-h1q-make-https-stream-traskriver-com-trask-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy-stream.sh
  - scripts/configure-stream-ols-route.sh
  - scripts/verify-stream-route.sh
autonomous: true
requirements:
  - QUICK-HTTPS-STREAM-01
must_haves:
  truths:
    - 'Remote HTTPS request to `https://stream.traskriver.com/trask/index.m3u8` returns 200 instead of 404.'
    - 'OLS/xCloud routes `/trask/*` to the stream app on `127.0.0.1:8088` with path preserved.'
    - 'Deploy flow enforces route setup before declaring stream deployment successful.'
  artifacts:
    - path: 'scripts/configure-stream-ols-route.sh'
      provides: 'Idempotent OLS route provisioning for `/trask/*` proxying to port 8088.'
    - path: 'scripts/deploy-stream.sh'
      provides: 'Deploy orchestration that applies routing config and reloads web server safely.'
    - path: 'scripts/verify-stream-route.sh'
      provides: 'Automated smoke checks for local upstream and public HTTPS manifest reachability.'
  key_links:
    - from: 'OLS vhost route for stream.traskriver.com'
      to: 'http://127.0.0.1:8088/trask/index.m3u8'
      via: 'reverse-proxy context for `/trask/`'
      pattern: "proxy.*127\\.0\\.0\\.1:8088.*\\/trask"
    - from: 'scripts/deploy-stream.sh'
      to: 'scripts/configure-stream-ols-route.sh'
      via: 'deploy hook before health checks'
      pattern: "configure-stream-ols-route\\.sh"
---

<objective>
Fix remote HTTPS HLS access so `https://stream.traskriver.com/trask/index.m3u8` is reachable publicly without 404 by wiring xCloud/OpenLiteSpeed routing to the stream service on port 8088.

Purpose: Restore externally reachable playback path after self-hosted stream migration.
Output: Deterministic deploy + route-provision scripts with automated post-deploy route verification.
</objective>

<execution_context>
@/Users/jspn/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/jspn/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@scripts/deploy-stream.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add idempotent OLS/xCloud route provisioning for `/trask/*`</name>
  <files>scripts/configure-stream-ols-route.sh</files>
  <action>Create a deploy-safe shell script that writes/updates the site-level OLS route config for `stream.traskriver.com` so `/trask/*` reverse-proxies to `http://127.0.0.1:8088/trask/*` (path preserved, host and forwarding headers set, websocket upgrades disabled). The script must be idempotent and fail-fast if expected OLS/xCloud paths are missing, instead of silently succeeding.</action>
  <verify>
    <automated>bash scripts/configure-stream-ols-route.sh --check</automated>
  </verify>
  <done>Running the script twice yields no diff on second run and leaves active OLS route config for `/trask/` targeting `127.0.0.1:8088`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire routing setup into stream deployment flow</name>
  <files>scripts/deploy-stream.sh</files>
  <action>Update deploy orchestration to call `scripts/configure-stream-ols-route.sh` after build artifacts are ready and before final service checks. Add safe OLS reload/restart handling (only if config changed) and explicit logging around route application. Do not re-enable unrelated disabled systemd blocks in this task.</action>
  <verify>
    <automated>bash scripts/deploy-stream.sh</automated>
  </verify>
  <done>Deploy execution applies/validates route configuration and exits non-zero if route provisioning or OLS reload fails.</done>
</task>

<task type="auto">
  <name>Task 3: Add public-route smoke verification script</name>
  <files>scripts/verify-stream-route.sh</files>
  <action>Create a script that verifies both upstream and public accessibility: (1) local upstream `http://127.0.0.1:8088/trask/index.m3u8` returns 200 and contains `#EXTM3U`; (2) public HTTPS URL `https://stream.traskriver.com/trask/index.m3u8` returns 200 and contains `#EXTM3U`; (3) script prints actionable failure messages for 404/502/timeout. Keep checks curl-based and non-interactive.</action>
  <verify>
    <automated>bash scripts/verify-stream-route.sh</automated>
  </verify>
  <done>Script exits 0 only when both local and public manifest endpoints are valid HLS manifests, otherwise exits non-zero with a clear failure cause.</done>
</task>

</tasks>

<verification>
Execute deploy, then run route smoke script. Confirm remote manifest fetch no longer returns 404 and includes HLS header.
</verification>

<success_criteria>

- `https://stream.traskriver.com/trask/index.m3u8` responds with HTTP 200.
- Response body begins with `#EXTM3U`.
- Re-running deploy is idempotent and does not break existing stream process.
  </success_criteria>

<output>
After completion, create `.planning/quick/260423-h1q-make-https-stream-traskriver-com-trask-i/260423-h1q-SUMMARY.md`
</output>
