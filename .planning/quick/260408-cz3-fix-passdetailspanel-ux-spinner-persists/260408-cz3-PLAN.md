---
phase: quick
plan: 260408-cz3
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/components/PassDetailsPanel.svelte
autonomous: true
requirements: []

must_haves:
  truths:
    - "unavailable phase shows no spinner — button is clickable with primary color"
    - "starting phase still shows spinner and secondary/90 background"
    - "pulse connecting message does not appear during unavailable phase"
  artifacts:
    - path: "packages/web/src/lib/components/PassDetailsPanel.svelte"
      provides: "Corrected isStarting derived — excludes unavailable"
      contains: "phase === 'starting'"
  key_links:
    - from: "isStarting derived (line 27)"
      to: "spinner condition (line 101)"
      via: "isStarting reference"
      pattern: "isStarting \\|\\| demandLoading"
    - from: "isStarting derived (line 27)"
      to: "button color ternary (line 93)"
      via: "isStarting reference"
      pattern: "bg-secondary/90"
---

<objective>
Fix two UX regressions in PassDetailsPanel where the `unavailable` phase was incorrectly folded into the `isStarting` derived state.

Purpose: `unavailable` is a retry-CTA state — the button must be clickable and primary-colored with no spinner. Including it in `isStarting` caused (1) a persistent spinner and (2) a secondary-colored disabled-looking button, making the retry action invisible to users.

Output: Single-line fix to `isStarting` derived in PassDetailsPanel.svelte that corrects all three dependent expressions (spinner, button color, pulse message) in one change.
</objective>

<execution_context>
@/Users/jspn/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/jspn/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/web/src/lib/components/PassDetailsPanel.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unavailable from isStarting derived</name>
  <files>packages/web/src/lib/components/PassDetailsPanel.svelte</files>
  <action>
    On line 27, change:

    ```svelte
    let isStarting = $derived(phase === 'starting' || phase === 'unavailable');
    ```

    to:

    ```svelte
    let isStarting = $derived(phase === 'starting');
    ```

    This single change fixes all three downstream expressions that reference `isStarting`:
    - Line 82: `{#if isStarting && demandRegistered}` — pulse message will no longer show during `unavailable`
    - Line 93: `{isStarting || demandLoading ? 'bg-secondary/90' : ...}` — button color will fall through to `bg-primary` during `unavailable`
    - Line 101: `{#if isStarting || demandLoading}` — spinner will not render during `unavailable`

    No other changes required. `buttonDisabled` already excludes `unavailable` (line 28) and `ctaLabel` already has a dedicated `unavailable` branch (line 35).
  </action>
  <verify>
    <automated>cd /Users/jspn/Documents/Sites/river-stream && bun check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `isStarting` derived contains only `phase === 'starting'`
    - `bun check` passes with no type errors
    - `unavailable` phase: no spinner, primary-colored button, no pulse message
    - `starting` phase: spinner visible, secondary/90 button, pulse message shown (unchanged behavior)
  </done>
</task>

</tasks>

<verification>
After the fix, manually verify state transitions by reviewing the component logic:

- `unavailable`: `isStarting=false`, `buttonDisabled=false` → button renders with `bg-primary`, cursor-pointer, no spinner
- `starting`: `isStarting=true`, `buttonDisabled=true` → button renders with `bg-secondary/90`, spinner visible
- `demandLoading=true`: `isStarting` irrelevant → spinner and secondary/90 still fire via `demandLoading` branch (unchanged)
</verification>

<success_criteria>
`isStarting` no longer includes `unavailable`. The retry CTA in `unavailable` phase is visually primary and actionable. The `starting` phase spinner behavior is unchanged.
</success_criteria>

<output>
After completion, create `.planning/quick/260408-cz3-fix-passdetailspanel-ux-spinner-persists/260408-cz3-SUMMARY.md`
</output>
