---
phase: 06-mediamtx-supervisor-rtsp-ingest
plan: 04
type: execute
wave: 4
depends_on: ['06-03']
files_modified: []
autonomous: true
requirements: [STRM-02, STRM-03, STRM-04, STRM-05, STRM-06, STRM-07]
must_haves:
  truths:
    - 'bun run build --filter=@traskriver/stream exits 0 and emits dist/{index,config,logger,server,supervisor,mediamtx-config,mediamtx-api}.js'
    - 'node --check on every emitted dist/*.js exits 0 (no parse errors after tsc rewrites .ts → .js specifiers)'
    - 'bun check passes repo-wide (no orphaned types, no breakage in packages/web/relay/shared)'
    - 'Phase 6 SUMMARY rolls up plans 01-04, STATE.md and ROADMAP.md reflect completion, phase commit lands on the branch'
  artifacts:
    - path: packages/stream/dist/supervisor.js
      provides: compiled Supervisor class (parses under node --check)
    - path: packages/stream/dist/mediamtx-config.js
      provides: compiled yaml generator
    - path: packages/stream/dist/mediamtx-api.js
      provides: compiled REST API poller
    - path: packages/stream/dist/index.js
      provides: compiled boot entry that imports all of the above
    - path: .planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-SUMMARY.md
      provides: phase rollup
  key_links:
    - from: packages/stream/dist/index.js
      to: packages/stream/dist/supervisor.js
      via: 'rewritten import specifier "./supervisor.js" (tsc emits .js for .ts source imports)'
    - from: packages/stream/dist/supervisor.js
      to: packages/stream/dist/mediamtx-config.js + dist/mediamtx-api.js
      via: rewritten import specifiers ./mediamtx-config.js + ./mediamtx-api.js
---

<objective>
Prove the Phase 6 implementation builds, parses, type-checks across the workspace, and document the closing state. No NEW source files in `packages/stream/` — this plan runs the gate commands, lands the phase SUMMARY, and updates STATE/ROADMAP.

Purpose: Close the goal-backward verification loop — every truth in STRM-02..STRM-07 is observable through static gates (build + tsc + bun check). The dynamic gates (kill -9 backoff, stall watchdog, codec rejection, manifest serving) require a running MediaMTX binary + RTSP source and are listed under "Manual Smoke (optional)" — Plan execution does not block on them, but they're documented for the user to run locally before Phase 7.

Output: Validated `dist/` artifacts; `06-SUMMARY.md`; updated STATE/ROADMAP; phase commit.
</objective>

<execution_context>
@/Users/jspn/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/jspn/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-CONTEXT.md
@.planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-RESEARCH.md
@.planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-01-PLAN.md
@.planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-02-PLAN.md
@.planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-03-PLAN.md
@.planning/phases/05-packages-stream-skeleton/05-03-PLAN.md
@packages/stream/package.json
@packages/stream/tsconfig.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build, parse-check, and bun check</name>
  <files></files>
  <read_first>
    - packages/stream/package.json (build script: tsc; check script: tsc --noEmit)
    - packages/stream/tsconfig.json (rewriteRelativeImportExtensions on; outDir ./dist)
    - .planning/phases/05-packages-stream-skeleton/05-03-SUMMARY.md (precedent — same gate ran for Phase 5; note the bash-vs-zsh fail-fast caveat documented there if any inline scripting is needed)
  </read_first>
  <action>
Run the three allowed project commands in this exact order. Do NOT add new files; do NOT touch source.

**Step 1 — Clean rebuild of `packages/stream`:**

```bash
rm -rf packages/stream/dist
bun run build --filter=@traskriver/stream
```

Expected: exit 0; `packages/stream/dist/` contains `index.js`, `config.js`, `logger.js`, `server.js`, `supervisor.js`, `mediamtx-config.js`, `mediamtx-api.js` (plus `.js.map` siblings).

**Step 2 — `node --check` every emitted file:**

```bash
for f in packages/stream/dist/*.js; do
  node --check "$f" || { echo "node --check FAILED: $f"; exit 1; }
done
```

Expected: exit 0 with no output. Each file must parse — proves tsc's `rewriteRelativeImportExtensions` correctly rewrote every `./foo.ts` source import to `./foo.js` in emit (Phase 5 caught a single-vs-double-quote regression here — re-run handles both).

**Step 3 — Repo-wide `bun check`:**

```bash
bun check
```

Expected: exit 0. This catches:

- Phase 6 changes that accidentally widened `HealthStatus` and broke Plan 02's contract
- New imports from `packages/shared` that drag in deleted types (shouldn't happen in Phase 6, but the gate proves it)
- Any silent regression in `packages/web` or `packages/relay` from the Phase 6 work (should be none — Phase 6 only touches `packages/stream/src/`)

**If any step fails:** STOP. Do NOT proceed to Task 2. Report the failure with the exact command, exit code, and last 30 lines of output to the user. Failures here mean a Plan 01/02/03 task introduced a regression — escalate, do not paper over.
</action>
<verify>
<automated>rm -rf packages/stream/dist && bun run build --filter=@traskriver/stream && for f in packages/stream/dist/_.js; do node --check "$f" || exit 1; done && bun check</automated>
</verify>
<acceptance_criteria> - `packages/stream/dist/index.js` exists after build - `packages/stream/dist/supervisor.js` exists after build - `packages/stream/dist/mediamtx-config.js` exists after build - `packages/stream/dist/mediamtx-api.js` exists after build - `packages/stream/dist/config.js` exists after build - `packages/stream/dist/logger.js` exists after build - `packages/stream/dist/server.js` exists after build - Every `packages/stream/dist/_.js`passes`node --check`(loop exits 0)
    -`bun check`exits 0
    - Emit uses`.js`specifiers in import statements:`grep -E "from ['\"]\\./(supervisor|mediamtx-config|mediamtx-api|config|logger|server)['\"]" packages/stream/dist/index.js`exits non-zero (would match`.ts`specifiers — which would be a tsc rewrite failure); but`grep -E "from ['\"]\\./(supervisor|mediamtx-config|mediamtx-api|config|logger|server)\\.js['\"]" packages/stream/dist/index.js`exits 0
    -`node --check packages/stream/dist/index.js`reports no syntax errors
  </acceptance_criteria>
  <done>All three static gates green: build emits 7 files, every file parses under node --check,`bun check` clean across the monorepo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 06-SUMMARY.md, update STATE.md + ROADMAP.md, commit phase</name>
  <files>.planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/05-packages-stream-skeleton/05-SUMMARY.md (precedent — frontmatter shape, "Plans" table, "Decisions locked", "Forward-compat" sections)
    - .planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-01-SUMMARY.md (created at end of Plan 01)
    - .planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-02-SUMMARY.md (created at end of Plan 02)
    - .planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-03-SUMMARY.md (created at end of Plan 03)
    - .planning/STATE.md (current shape — extend "Current Position", "Recent Decisions", advance to Phase 7)
    - .planning/ROADMAP.md (current — flip Phase 6 to ✅, increment progress table, set Plans count to 4 with all checked)
  </read_first>
  <action>
Three documentation updates, then one commit.

**Update 1 — `.planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-SUMMARY.md`** (new file). Use the Phase 5 SUMMARY as the template. Required content:

Frontmatter (mirror Phase 5 shape):

```yaml
---
phase: 06-mediamtx-supervisor-rtsp-ingest
plan: rollup
subsystem: stream-supervisor
tags: [node22, mediamtx, child_process, rtsp, hls, pino-redact]

requires:
  - phase: 05-packages-stream-skeleton
    provides: '@traskriver/stream skeleton (config, logger, server, index)'
provides:
  - 'MediaMTX supervisor (spawn/respawn, backoff, SIGTERM→10s→SIGKILL)'
  - 'Stall watchdog (5s poll, 75s threshold, ready:true gated)'
  - 'Codec guard (H264 strict equality, fatal exit on mismatch)'
  - 'MediaMTX yaml generator (literal template, hlsAlwaysRemux: yes, rtspTransport: tcp)'
  - 'MediaMTX REST API client (native fetch + AbortSignal.timeout)'
  - 'Live /health status from supervisor.getStatus() (no globals)'
  - 'Pino redaction for RTSP_URL'
  - 'STRM-02..STRM-07 satisfied'
affects:
  - Phase 7 /health expansion (consumes supervisor state internals)
  - Phase 8 deployment (assumes mediamtx on PATH; HLS_DIR aligns with RuntimeDirectory=stream)

tech-stack:
  added: 'none — all work via Node 22 built-ins (child_process, readline, fs/promises) + existing pino/zod/hono'
  patterns:
    - 'Discriminated-union state machine on supervisor instance'
    - 'Closure injection for /health getStatus (no module-level singletons)'
    - 'intentionalStop flag to disambiguate our SIGTERM from a crash'
    - 'Backoff reset gated by readyAt timestamp + 60s clean uptime'

key-files:
  created:
    - packages/stream/src/supervisor.ts
    - packages/stream/src/mediamtx-config.ts
    - packages/stream/src/mediamtx-api.ts
  modified:
    - packages/stream/src/config.ts (5 new env keys)
    - packages/stream/src/logger.ts (pino redact)
    - packages/stream/src/server.ts (createApp accepts getStatus)
    - packages/stream/src/index.ts (Supervisor in boot + shutdown)

key-decisions:
  - 'Backoff: 1→2→4→8→16→30s cap, reset to 1s after 60s clean ready uptime'
  - 'SIGTERM→10s→SIGKILL escalation in killChild()'
  - 'Stall: 15 consecutive 5s polls with Δbytes==0 while ready:true (75s)'
  - 'Codec guard: check ONCE at first ready, fatal process.exit(1) on mismatch'
  - 'rtspTransport: tcp (NOT obsolete sourceProtocol — corrected vs CONTEXT wording per RESEARCH Pitfall #1)'
  - 'hlsAlwaysRemux: yes added per RESEARCH Open Question #3 (warm muxer for 24/7)'
  - '/health payload UNCHANGED at { status } — Phase 7 widens; Phase 6 only changes the source of status to live supervisor read'

patterns-established:
  - 'Supervisor class owns child_process + timers + state — no globals, getStatus injected via closure'
  - 'mediamtx-config.ts and mediamtx-api.ts are pure modules — no side effects, easily testable in Phase 7+ if needed'
  - 'tsc rewriteRelativeImportExtensions verified across 7 emit files'

requirements-completed: [STRM-02, STRM-03, STRM-04, STRM-05, STRM-06, STRM-07]

duration: —
completed: <iso date today>
---
```

Body sections (concise — Phase 5 SUMMARY is the model, ≤50 lines body):

1. `# Phase 6 rollup: MediaMTX Supervisor + RTSP Ingest` — one-line headline
2. `## Plans` table — 06-01 through 06-04 with one-line summary each
3. `## Files created` — list the 3 new src files + 4 modified
4. `## Decisions locked` — bullet the 7 from frontmatter `key-decisions` with brief context
5. `## Scope deferred to later phases` — explicit list (cache-headers → Phase 8 reverse proxy; full /health payload → Phase 7; systemd/TLS/DNS → Phase 8; binary provisioning → Phase 8) — protects against backslide
6. `## Manual smoke (optional, recommended before Phase 7)` — list the 5 dynamic checks from CONTEXT §Testing/Verification (kill -9 backoff, camera unplug ≥90s, force H.265, curl manifest, HLS files under HLS_DIR) with the ffmpeg-publisher command from RESEARCH §10
7. `## Forward-compat for Phase 7+` — internal supervisor state Plan 02 already tracks (codec, restart count, last-byte-ts) that Phase 7 will surface in /health
8. `## Deviations` — note that `EXT-X-DISCONTINUITY` (criterion #2) is not literally emitted; behavior is functionally equivalent (manifest reset + new prefix); validated via hls.js in Phase 9. Note that cache headers (criterion #4 prior wording) moved to Phase 8 per ROADMAP amendment after research.
9. `## Next` — "Phase 7 — full /health payload + shared-types purge"

**Update 2 — `.planning/STATE.md`** — minimal edits:

- Bump frontmatter `progress.completed_phases` to 2, `total_plans` and `completed_plans` accordingly
- `stopped_at: 'Phase 6 complete'`, `last_updated` to today's ISO date
- `## Current Position`: "Phase: 06 ... — **COMPLETE**", "Plan: 4 of 4 done — SUMMARY: `06-04-SUMMARY.md`, rollup: `06-SUMMARY.md`"
- `### Recent Decisions`: prepend a "Phase 6 supervisor: backoff 1→30s, stall 75s, codec guard fatal" line
- `## Session Continuity → Last Session`: today's date + activity "Executed 06-04-PLAN.md (build/check/SUMMARY/STATE/ROADMAP)"
- `### Next Session Should`: "Plan or execute Phase 7 — /health payload expansion + shared-types purge"

**Update 3 — `.planning/ROADMAP.md`**:

- Flip Phase 6 row in the bullet list `- [ ]` → `- [x]`
- Update Phase 6 `**Plans**: TBD` to `**Plans**: 4 plans` and add the 4-bullet checklist (`- [x] 06-01-PLAN.md — foundation modules ...` etc.)
- Update the Progress table row for "6. MediaMTX Supervisor + RTSP Ingest": `Plans Complete: 4/4`, `Status: Complete`, `Completed: <today>`
- Update footer `_Roadmap updated:_` line

**Update 4 — Phase commit:**

Stage and commit only the changes from this phase's work:

```bash
git add packages/stream/src/config.ts packages/stream/src/logger.ts packages/stream/src/server.ts packages/stream/src/index.ts packages/stream/src/supervisor.ts packages/stream/src/mediamtx-config.ts packages/stream/src/mediamtx-api.ts .planning/phases/06-mediamtx-supervisor-rtsp-ingest/ .planning/STATE.md .planning/ROADMAP.md
git commit -m "feat(stream): MediaMTX supervisor + RTSP ingest (Phase 6)

Spawn/backoff/SIGTERM-SIGKILL supervisor, stall watchdog, codec guard,
yaml generator, REST API client, Pino redact, /health live status.
STRM-02..STRM-07."
```

Do NOT push. Branch-based delivery per v1.2 milestone scope.

If `bun check` was already invoked in Task 1 and passed, do not re-run unless any file has changed since.
</action>
<verify>
<automated>test -f .planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-SUMMARY.md && grep -q "Phase 6 rollup" .planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-SUMMARY.md && grep -q "STRM-02, STRM-03, STRM-04, STRM-05, STRM-06, STRM-07" .planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-SUMMARY.md && grep -qE "^- \[x\] \*\*Phase 6:" .planning/ROADMAP.md && grep -q "Phase 6 complete" .planning/STATE.md && git log -1 --pretty=%s | grep -q "Phase 6"</automated>
</verify>
<acceptance_criteria> - `.planning/phases/06-mediamtx-supervisor-rtsp-ingest/06-SUMMARY.md` exists - SUMMARY frontmatter contains `requirements-completed: [STRM-02, STRM-03, STRM-04, STRM-05, STRM-06, STRM-07]` - SUMMARY body has all 9 required sections (Plans, Files, Decisions locked, Scope deferred, Manual smoke, Forward-compat, Deviations, Next, plus the headline `# Phase 6 rollup` line) - SUMMARY explicitly mentions cache-header deferral to Phase 8 reverse proxy AND EXT-X-DISCONTINUITY behavioral equivalence (these are the two RESEARCH open questions resolved by ROADMAP amendment) - `.planning/STATE.md` `Current Position` line includes "Phase: 06 ... — **COMPLETE**" (case-insensitive ok) and `last_updated` is today - `.planning/STATE.md` Next Session Should line points to Phase 7 - `.planning/ROADMAP.md` has `- [x] **Phase 6:` (checked) and `**Plans**: 4 plans` for Phase 6 - `.planning/ROADMAP.md` Progress table row for Phase 6 shows `4/4` and `Complete` - `git log -1 --pretty=%s` contains "Phase 6" (commit landed) - `git status` shows no untracked Phase 6 plan/summary files (everything committed)
</acceptance_criteria>
<done>06-SUMMARY.md complete with all 9 sections + correct frontmatter; STATE.md advanced to Phase 7; ROADMAP.md Phase 6 row checked + plans listed; phase commit on the branch.</done>
</task>

</tasks>

<verification>
After both tasks:
1. Build green: `ls packages/stream/dist/*.js | wc -l` reports ≥ 7
2. Parse green: every dist file passes `node --check`
3. Workspace green: `bun check` exits 0
4. Docs synced: STATE / ROADMAP / SUMMARY all reflect Phase 6 closed
5. History: `git log -1 --pretty=%B` shows the Phase 6 commit message with STRM IDs
</verification>

<success_criteria>
**Static (hard gates this plan enforces):**

- `bun run build --filter=@traskriver/stream` exits 0
- Every emitted `dist/*.js` parses under `node --check`
- `bun check` exits 0 repo-wide
- Phase 6 SUMMARY + STATE + ROADMAP committed

**Dynamic (manual smoke documented in SUMMARY, not blocking this plan):**

- `kill -9 $(pgrep mediamtx)` triggers supervisor respawn with growing backoff
- Forcing camera to H.265 causes `FATAL: camera codec is H265, expected H264` log + non-zero exit
- `kill -STOP <ffmpeg-pid>` (or unplug camera) for 75s+ triggers stall-restart
- `curl http://localhost:8888/trask/index.m3u8` returns a manifest with 6 entries and 2s `EXTINF` durations
- HLS `.ts` segments appear under `${HLS_DIR}`

These are listed in the SUMMARY's "Manual smoke" section and are owner-run before Phase 7 — Plan 04 does not block on them because they require a real MediaMTX binary + RTSP source, which is a Phase 8 deployment concern.
</success_criteria>

<output>
After completion, this plan IS the closing artifact. The 06-SUMMARY.md and the phase commit are the deliverables. Plan 04 does NOT produce its own per-plan SUMMARY (the rollup IS the per-plan summary).
</output>
