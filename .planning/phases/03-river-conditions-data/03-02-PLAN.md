---
phase: 03-river-conditions-data
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/web/src/routes/+page.svelte
autonomous: false
requirements:
  - FOOT-01

must_haves:
  truths:
    - 'RiverConditions and FishRunStatus components are visible in the sidebar between LocalWeather and the sticky CTA'
    - 'TelemetryFooter (encoding/bitrate) is no longer displayed'
    - 'The sidebar scrolls to accommodate all content on mobile drawer'
    - 'All stream phase transitions still work correctly (idle → starting → viewing → ended)'
  artifacts:
    - path: 'packages/web/src/routes/+page.svelte'
      provides: 'Sidebar with river conditions components wired in, TelemetryFooter removed'
      contains: 'RiverConditions'
  key_links:
    - from: 'packages/web/src/routes/+page.svelte'
      to: 'packages/web/src/lib/components/RiverConditions.svelte'
      via: 'import and render in sidebar'
      pattern: 'import RiverConditions from'
    - from: 'packages/web/src/routes/+page.svelte'
      to: 'packages/web/src/lib/components/FishRunStatus.svelte'
      via: 'import and render in sidebar'
      pattern: 'import FishRunStatus from'
---

<objective>
Wire river conditions components into the sidebar and replace the telemetry footer.

Purpose: Integrate the data components built in Plan 01 into the live page sidebar. Replace the old TelemetryFooter (encoding/bitrate — irrelevant to anglers) with the new river data components. This completes the Phase 3 user experience.

Output: Modified `+page.svelte` with new component imports and placement, TelemetryFooter import + usage removed.
</objective>

<execution_context>
@/Users/jspn/Documents/Sites/traskriver.com/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jspn/Documents/Sites/traskriver.com/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-river-conditions-data/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Current sidebar structure in +page.svelte (lines 465-553):

```svelte
<!-- Scrollable content area -->
<div class="flex min-w-0 flex-1 flex-col">
	<div class="px-5 pt-8 pb-6 md:px-12 md:pt-16 md:pb-8">
		<h2>Trask River Cam</h2>
		<p>Get a live look at the Trask River...</p>
	</div>
	<LocalWeather />
	<!-- KEEP -->
</div>

<!-- Sticky bottom area -->
<div class="sticky bottom-0 z-10 overflow-visible! ...">
	<div class="px-5 py-4 md:px-12 md:py-6">
		<!-- CTA button content -->
		<!-- KEEP -->
	</div>
	<TelemetryFooter />
	<!-- REMOVE -->
</div>
```

Current imports (lines 3-5):

```svelte
import VideoPlayer from '$lib/components/VideoPlayer.svelte'; import TelemetryFooter from
'$lib/components/TelemetryFooter.svelte'; import LocalWeather from
'$lib/components/LocalWeather.svelte';
```

New components to add (created by Plan 01):

- `$lib/components/RiverConditions.svelte` — USGS gauge data
- `$lib/components/FishRunStatus.svelte` — fish run status
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire river conditions components and remove TelemetryFooter</name>
  <files>packages/web/src/routes/+page.svelte</files>

<read_first> - packages/web/src/routes/+page.svelte (FULL file — understand all sidebar structure before editing) - packages/web/src/lib/components/RiverConditions.svelte (verify it exists and confirm import path) - packages/web/src/lib/components/FishRunStatus.svelte (verify it exists and confirm import path)
</read_first>

  <action>
**Three changes to `packages/web/src/routes/+page.svelte`:**

**Change 1: Update imports (top of `<script>` block)**

Remove the TelemetryFooter import:

```
DELETE: import TelemetryFooter from '$lib/components/TelemetryFooter.svelte';
```

Add two new imports after the LocalWeather import:

```typescript
import RiverConditions from '$lib/components/RiverConditions.svelte';
import FishRunStatus from '$lib/components/FishRunStatus.svelte';
```

**Change 2: Add components in the scrollable sidebar content area**

Currently the scrollable content `<div>` (class `flex min-w-0 flex-1 flex-col`) contains:

1. Branding header `<div>`
2. `<LocalWeather />`

After `<LocalWeather />` and before the closing `</div>` of the scrollable content area, add:

```svelte
<RiverConditions />
<FishRunStatus />
```

This places the new components in the scrollable area above the sticky CTA, ensuring they scroll naturally on mobile.

**Change 3: Remove TelemetryFooter usage**

Find `<TelemetryFooter />` (near end of file, inside the sticky bottom area) and delete it entirely. The sticky bottom area should only contain the CTA button section.

**CRITICAL: Do NOT modify any stream logic, phase state machine, polling code, demand/relay code, or CTA button behavior.** Only touch imports and the sidebar content structure.

After editing, run `bun format` from `packages/web/` to ensure Prettier consistency.
</action>

  <verify>
    <automated>grep -q "RiverConditions" packages/web/src/routes/+page.svelte && grep -q "FishRunStatus" packages/web/src/routes/+page.svelte && ! grep -q "TelemetryFooter" packages/web/src/routes/+page.svelte && echo "PASS"</automated>
  </verify>

<acceptance_criteria> - +page.svelte contains `import RiverConditions from '$lib/components/RiverConditions.svelte'` - +page.svelte contains `import FishRunStatus from '$lib/components/FishRunStatus.svelte'` - +page.svelte does NOT contain `TelemetryFooter` anywhere (import removed, usage removed) - `<RiverConditions />` appears after `<LocalWeather />` in the scrollable content div - `<FishRunStatus />` appears after `<RiverConditions />` in the scrollable content div - Stream phase state machine code is unchanged (no modifications to phase transitions, polling, demand logic) - File passes Prettier formatting (`bun format` from packages/web)
</acceptance_criteria>

<done>+page.svelte imports and renders RiverConditions + FishRunStatus in the sidebar scrollable area; TelemetryFooter import and usage completely removed; no stream logic modified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of river conditions in sidebar</name>
  <files>packages/web/src/routes/+page.svelte</files>
  <action>
**What was built:**
Complete Phase 3 river conditions integration:
- Sidebar now shows: Branding → Weather (with sunrise/sunset) → River Conditions (flow cfs, water temp °F, freshness timestamp) → Fish Runs (species in season with peak badges) → Sticky CTA button
- Old telemetry footer (encoding/bitrate) is gone
- All data fetches from public APIs (Open-Meteo, USGS) on component mount

**How to verify:**

1. Run `bun dev` from repo root (or `bun --cwd packages/web dev`)
2. Open http://localhost:5173 in browser
3. Check sidebar content (desktop: right panel; mobile: bottom drawer):
   a. Weather section should show Sunrise and Sunset times (e.g., "6:32 AM", "7:58 PM") in the grid alongside Wind, Humidity, Precipitation
   b. River Conditions section should show "River Flow" in cfs and "Water Temp" in °F with an "Updated X min ago" freshness line
   c. Fish Runs section should show species currently in season with "Peak" badges where applicable
   d. Telemetry footer (Encoding: H.264, Max Bitrate: 4.096 Mbps) should be GONE
4. If USGS gauge is temporarily offline, River Conditions should show "Gauge data temporarily unavailable" instead of crashing
5. Test mobile drawer — all content should scroll naturally, CTA button remains sticky at bottom
6. Test stream cycle: click "Start Stream" → stream connects → "Streaming" state → timeout → "Watch Again" — all phases should work exactly as before

→ Type "approved" or describe issues
</action>
<verify>User visually confirms sidebar displays weather with sunrise/sunset, river conditions with freshness, fish runs with peak badges, and no telemetry footer</verify>
<done>User approves visual verification — all river data components render correctly in sidebar, TelemetryFooter gone, stream transitions unaffected</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                      |
| -------- | -------------------------------------------------------------------------------- |
| N/A      | This plan only wires existing components — no new data flows or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category          | Component                    | Disposition | Mitigation Plan                                                             |
| --------- | ----------------- | ---------------------------- | ----------- | --------------------------------------------------------------------------- |
| T-03-05   | Denial of Service | +page.svelte component mount | accept      | Components fetch once on mount (established pattern), no amplification risk |

</threat_model>

<verification>
1. `grep "RiverConditions" packages/web/src/routes/+page.svelte` — component imported and used
2. `grep "FishRunStatus" packages/web/src/routes/+page.svelte` — component imported and used
3. `! grep "TelemetryFooter" packages/web/src/routes/+page.svelte` — completely removed
4. `bun run check` from repo root — TypeScript passes
5. Visual inspection confirms sidebar shows weather + river data + fish runs
</verification>

<success_criteria>

- TelemetryFooter is completely removed from +page.svelte (import and usage)
- RiverConditions and FishRunStatus render in sidebar between LocalWeather and sticky CTA
- All stream phase transitions work correctly (no regression)
- Mobile drawer scrolls smoothly with additional content
- Visual verification approved by user
  </success_criteria>

<output>
After completion, create `.planning/phases/03-river-conditions-data/03-02-SUMMARY.md`
</output>
