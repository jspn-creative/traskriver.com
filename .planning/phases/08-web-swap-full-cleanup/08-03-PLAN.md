---
phase: 08-web-swap-full-cleanup
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - packages/relay/
  - .github/workflows/deploy-relay.yml
  - package.json
  - turbo.json
autonomous: true
requirements:
  - CLEAN-03
  - CLEAN-05

must_haves:
  truths:
    - 'packages/relay/ directory is completely deleted'
    - '.github/workflows/deploy-relay.yml is deleted'
    - 'Root package.json workspaces array does not include packages/relay'
    - 'Root package.json scripts have no relay references (dev:relay, dev:all, build:relay, check:relay)'
    - 'turbo.json has no relay-specific config (currently wildcard — likely no changes needed)'
    - 'bun check passes repo-wide'
    - 'bun lint passes repo-wide'
  artifacts:
    - path: 'package.json'
      provides: 'Clean root package.json with no relay workspace or scripts'
    - path: 'turbo.json'
      provides: 'Clean turbo config (verify no relay-specific entries)'
  key_links:
    - from: 'package.json'
      to: 'packages/stream'
      via: 'workspaces array includes packages/stream but not packages/relay'
      pattern: 'packages/stream'
---

<objective>
Delete packages/relay, its GitHub Actions workflow, clean workspace references, and verify repo-wide type checking + linting passes.

Purpose: Complete the full cleanup by removing the relay package entirely, cleaning all workspace-level references, and running the final green-light verification (`bun check` + `bun lint`) that proves the entire phase is complete with no orphaned references.

Output: Relay package deleted, workspace cleaned, repo-wide checks pass.
</objective>

<execution_context>
@/Users/jspn/Documents/Sites/traskriver.com/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jspn/Documents/Sites/traskriver.com/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-web-swap-full-cleanup/08-CONTEXT.md
@.planning/phases/08-web-swap-full-cleanup/08-01-SUMMARY.md
@.planning/phases/08-web-swap-full-cleanup/08-02-SUMMARY.md
</context>

<interfaces>
<!-- Key files being modified — extracted from codebase -->

Root package.json (current state):

```json
{
	"workspaces": ["packages/web", "packages/relay", "packages/shared", "packages/stream"],
	"scripts": {
		"dev:relay": "bun run --cwd packages/relay dev:local",
		"dev:all": "concurrently -n web,relay -c blue,magenta \"bun run --cwd packages/web dev\" \"bun run --cwd packages/relay dev:local\"",
		"build:relay": "bun run --cwd packages/relay build",
		"check:relay": "bun run --cwd packages/relay check"
		// ... other scripts to keep
	}
}
```

turbo.json (current state — uses wildcards, likely no changes):

```json
{
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".svelte-kit/cloudflare/**", "packages/stream/dist/**"]
		},
		"check": {}
	}
}
```

packages/relay/ directory contents:

- config/, scripts/, src/, .env.example, .gitignore, package.json, README.md, tsconfig.json
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete packages/relay and its GitHub Actions workflow</name>
  <files>packages/relay/, .github/workflows/deploy-relay.yml</files>
  <read_first>
    - .github/workflows/deploy-relay.yml (verify exists)
  </read_first>
  <action>
**Delete the entire `packages/relay/` directory and its CI workflow (per D-16, CLEAN-03):**

```bash
git rm -rf packages/relay/
git rm .github/workflows/deploy-relay.yml
```

**Verify no remaining references to `packages/relay` or `@traskriver/relay` in the codebase (excluding .planning/ docs):**

```bash
grep -rn "packages/relay\|@traskriver/relay" --include="*.ts" --include="*.json" --include="*.jsonc" --include="*.yml" --include="*.yaml" --include="*.svelte" . 2>/dev/null | grep -v ".planning/" | grep -v "node_modules/"
```

This should return matches only in root `package.json` (to be cleaned in Task 2).

**After deletion, check if `.github/workflows/` still has other workflow files:**

```bash
ls .github/workflows/
```

If `release-drafter.yml` is the only remaining file, the directory stays. If the directory would be empty, `git rm -rf` would have removed it.
</action>
<verify>
<automated>test ! -d packages/relay && test ! -f .github/workflows/deploy-relay.yml && echo "Relay package and workflow deleted"</automated>
</verify>
<acceptance_criteria> - `packages/relay/` directory does not exist - `.github/workflows/deploy-relay.yml` does not exist - `.github/workflows/release-drafter.yml` still exists (unaffected) - `grep -rn "@traskriver/relay" --include="*.ts" --include="*.svelte" . 2>/dev/null | grep -v ".planning/" | grep -v "node_modules/"` returns no matches
</acceptance_criteria>
<done>packages/relay/ entirely deleted including all subdirectories. deploy-relay.yml workflow deleted. No code references remain to @traskriver/relay.</done>
</task>

<task type="auto">
  <name>Task 2: Clean root package.json workspace refs and relay scripts</name>
  <files>package.json, turbo.json</files>
  <read_first>
    - package.json (root — current state with relay workspace and scripts)
    - turbo.json (verify wildcard tasks don't need changes)
  </read_first>
  <action>
**Edit root `package.json`:**

1. Remove `"packages/relay"` from the `workspaces` array. Result:

```json
"workspaces": [
  "packages/web",
  "packages/shared",
  "packages/stream"
]
```

2. Remove relay-specific scripts (per D-16):
   - DELETE `"dev:relay": "bun run --cwd packages/relay dev:local"`
   - DELETE `"dev:all": "concurrently -n web,relay -c blue,magenta \"bun run --cwd packages/web dev\" \"bun run --cwd packages/relay dev:local\""`
   - DELETE `"build:relay": "bun run --cwd packages/relay build"`
   - DELETE `"check:relay": "bun run --cwd packages/relay check"`

3. Keep ALL other scripts unchanged: `dev`, `dev:web`, `dev:stream`, `build`, `build:stream`, `deploy`, `deploy:preview`, `preview`, `check`, `check:web`, `check:stream`, `start:stream`, `lint`, `format`, `gen`.

**Verify `turbo.json`:**
Read `turbo.json`. Currently it uses wildcard task definitions (`"build": {}`, `"check": {}`). The `outputs` array includes `"packages/stream/dist/**"` but no relay-specific entries. Confirm no changes needed — if there are any relay-specific entries, remove them.

The current `turbo.json` `build.outputs` has `"dist/**"` (catches all packages) and `".svelte-kit/cloudflare/**"` and `"packages/stream/dist/**"`. The `"dist/**"` wildcard already covered relay's dist. With relay deleted, no change needed. Leave turbo.json as-is.
</action>
<verify>
<automated>grep -c "relay" package.json | grep "^0$" && echo "No relay refs in package.json" && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const ws=p.workspaces; if(!ws.includes('packages/relay') && ws.includes('packages/web') && ws.includes('packages/shared') && ws.includes('packages/stream')) console.log('Workspaces correct'); else { console.error('Workspaces wrong:', ws); process.exit(1); }"</automated>
</verify>
<acceptance_criteria> - `package.json` `workspaces` array is exactly `["packages/web", "packages/shared", "packages/stream"]` - `package.json` contains NO scripts with "relay" in their name or value - `package.json` retains all non-relay scripts: dev, dev:web, dev:stream, build, build:stream, deploy, deploy:preview, preview, check, check:web, check:stream, start:stream, lint, format, gen - `turbo.json` is unchanged (wildcard tasks, no relay-specific config) - `grep -c "relay" package.json` returns 0
</acceptance_criteria>
<done>Root package.json cleaned: relay removed from workspaces, all relay scripts deleted, all other scripts preserved. turbo.json confirmed clean (wildcard tasks).</done>
</task>

<task type="auto">
  <name>Task 3: Run bun install, bun check, and bun lint to verify clean build</name>
  <files></files>
  <read_first>
    - package.json (to confirm workspace changes took effect)
  </read_first>
  <action>
**Run the full verification suite to confirm the entire phase is clean:**

1. **Reinstall dependencies** (workspace changed — relay removed):

```bash
bun install
```

This should complete without errors. The lockfile will update to remove relay's dependencies.

2. **Run repo-wide type checking:**

```bash
bun check
```

This runs `turbo run check` which runs `svelte-check` in `packages/web` and `tsc --noEmit` in `packages/stream` and `packages/shared`. All should pass with 0 errors.

3. **Run repo-wide linting:**

```bash
bun lint
```

This runs `prettier --check` in `packages/web`. Should pass.

4. **Verify no orphaned imports exist anywhere:**

```bash
grep -rn "stream\.remote\|LiveViewerCount\|lib/server/posthog\|lib/types\|@traskriver/relay\|RIVER_KV\|RELAY_API_TOKEN\|registerDemand\|pollRelayStatus\|prefetchRelayStatus\|CF_STREAM" --include="*.ts" --include="*.svelte" --include="*.jsonc" packages/ 2>/dev/null
```

Should return ZERO matches.

If `bun check` fails, investigate and fix the specific error. Common issues:

- Orphaned import in a file not caught by Plan 01/02 — delete the import
- Type error from removed Platform env — ensure app.d.ts is clean
- Missing `PUBLIC_STREAM_HLS_URL` in env validation — this is a build-time var, should be fine
  </action>
  <verify>
  <automated>bun check && bun lint && echo "All checks pass"</automated>
  </verify>
  <acceptance_criteria> - `bun install` completes without errors - `bun check` exits 0 with no type errors - `bun lint` exits 0 with no formatting issues - `grep -rn "stream\.remote\|@traskriver/relay\|RIVER_KV\|RELAY_API_TOKEN\|registerDemand\|pollRelayStatus" --include="*.ts" --include="*.svelte" --include="*.jsonc" packages/` returns no matches
  </acceptance_criteria>
  <done>Repo-wide bun check + bun lint pass green. No orphaned references to relay, demand, KV, or CF Stream in the codebase. Phase 8 is complete.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                        |
| -------- | ------------------------------------------------------------------ |
| N/A      | This plan deletes code and cleans config — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                                              |
| --------- | ---------------------- | --------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-08-05   | Tampering              | deploy-relay.yml deletion   | accept      | Workflow file deleted — no CI pipeline can trigger relay deployments. Relay package is fully deleted.                        |
| T-08-06   | Information Disclosure | packages/relay/.env.example | mitigate    | File is deleted with the entire packages/relay directory. The .env.example contained only template values (no real secrets). |

</threat_model>

<verification>
1. `test ! -d packages/relay` — relay package deleted
2. `test ! -f .github/workflows/deploy-relay.yml` — workflow deleted
3. `grep -c "relay" package.json` — returns 0
4. `bun check` — exits 0
5. `bun lint` — exits 0
6. `grep -rn "stream\.remote\|@traskriver/relay\|RIVER_KV\|RELAY_API_TOKEN" --include="*.ts" --include="*.svelte" --include="*.jsonc" packages/` — no matches
</verification>

<success_criteria>

- packages/relay/ entirely deleted
- deploy-relay.yml deleted
- Root package.json has no relay workspace or scripts
- turbo.json unchanged (already clean)
- bun check + bun lint pass repo-wide
- No orphaned relay/demand/KV/CF Stream references in any source file
  </success_criteria>

<output>
After completion, create `.planning/phases/08-web-swap-full-cleanup/08-03-SUMMARY.md`
</output>
