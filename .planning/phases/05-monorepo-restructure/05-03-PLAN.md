---
phase: 05-monorepo-restructure
plan: '03'
type: execute
wave: 2
depends_on:
  - '01'
  - '02'
files_modified:
  - package.json
  - .gitignore
  - bun.lock
autonomous: true
requirements:
  - MONO-03
  - MONO-04
user_setup: []

must_haves:
  truths:
    - 'bun dev from root runs both Vite dev server and relay process simultaneously'
    - 'wrangler deploy from /packages/web deploys to Cloudflare Workers successfully'
    - 'All packages resolve shared types from @river-stream/shared'
  artifacts:
    - path: package.json
      provides: Root workspace configuration
      contains: workspaces = ["packages/web", "packages/relay", "packages/shared"]
    - path: bun.lock
      provides: Lockfile for all workspace dependencies
      contains: resolved versions for all packages
  key_links:
    - from: package.json (root)
      to: packages/web, packages/relay, packages/shared
      via: workspaces array
      pattern: 'workspaces'
    - from: packages/relay/scripts/
      to: packages/relay/src/index.ts
      via: relay dev script
      pattern: 'dev:local'
---

<objective>
Wire the monorepo together: configure Bun workspaces at root, add dev scripts, install dependencies, and verify everything works end-to-end.
</objective>

<context>
@/Users/jspn/Documents/Sites/river-stream/05-CONTEXT.md
@/Users/jspn/Documents/Sites/river-stream/package.json
@/Users/jspn/Documents/Sites/river-stream/.gitignore
@/Users/jspn/Documents/Sites/river-stream/packages/web/wrangler.jsonc
@/Users/jspn/Documents/Sites/river-stream/packages/web/.gitignore
@/Users/jspn/Documents/Sites/river-stream/packages/relay/.gitignore
@/Users/jspn/Documents/Sites/river-stream/packages/shared/.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure root package.json with Bun workspaces and dev scripts</name>
  <files>package.json</files>
  <action>
Replace the root package.json with workspace configuration:

```json
{
	"name": "river-stream",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"workspaces": ["packages/web", "packages/relay", "packages/shared"],
	"scripts": {
		"dev": "bun --bun wxt concurrently \"bun run --cwd packages/web dev\" \"bun run --cwd packages/relay dev:local\"",
		"dev:web": "bun run --cwd packages/web dev",
		"dev:relay": "bun run --cwd packages/relay dev:local",
		"build": "bun run --cwd packages/web build",
		"build:relay": "bun run --cwd packages/relay build",
		"deploy": "bun run --cwd packages/web deploy",
		"preview": "bun run --cwd packages/web preview",
		"check": "bun run --cwd packages/web check",
		"check:relay": "bun run --cwd packages/relay check",
		"lint": "bun run --cwd packages/web lint",
		"format": "bun run --cwd packages/web format",
		"gen": "bun run --cwd packages/web gen"
	},
	"devDependencies": {
		"concurrently": "^9.0.0",
		"wrangler": "^4.63.0"
	}
}
```

**IMPORTANT:** Install `concurrently` as a devDependency at root. The `bun --bun wxt` prefix ensures Bun runs concurrently (or use `bun run -c` with a custom script). If `concurrently` is not ideal for Bun, use `bun x concurrently` instead of the npm version.

Alternative using bun's native capabilities — if concurrently isn't needed:

```json
"dev": "bun run --cwd packages/web dev & bun run --cwd packages/relay dev:local"
```

But use concurrently for proper process management with kill-on-exit behavior.
</action>
<verify>
<automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json')); const hasWs = Array.isArray(p.workspaces) && p.workspaces.length === 3; const hasDev = p.scripts.dev && p.scripts.dev.includes('packages/web') && p.scripts.dev.includes('packages/relay'); console.log(hasWs && hasDev ? 'ROOT_PKG_OK' : 'ROOT_PKG_FAIL')"</automated>
</verify>
<done>Root package.json has workspaces array, dev script runs both web and relay, deploy script targets packages/web</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore and install dependencies</name>
  <files>.gitignore</files>
  <action>
Update root .gitignore to account for monorepo structure:

Append to or replace .gitignore:

```
# Bun workspace
.bun/

# Individual package ignores are respected
packages/*/node_modules/
packages/*/.svelte-kit/
packages/*/dist/
packages/*/.env
packages/*/.env.*
packages/*/*.local

# Keep .env.example in packages
!packages/*/.env.example
```

Now install all dependencies from root:

```bash
bun install
```

This should:

1. Resolve workspace dependencies (packages/web and packages/relay both reference @river-stream/shared)
2. Generate bun.lock
3. Install concurrently at root
4. Link all workspace packages

After install, verify the lockfile exists and packages are linked:

```bash
ls -la bun.lock
bun pm ls  # should show @river-stream/shared in web and relay
```

</action>
  <verify>
<automated>test -f bun.lock && echo "LOCKFILE_EXISTS" && bun pm ls 2>/dev/null | grep -c "@river-stream" | xargs -I{} test {} -ge 2 && echo "WORKSPACE_LINKS_OK"</automated>
</verify>
  <done>bun.lock exists, all workspace packages resolve @river-stream/shared, root devDependencies installed</done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end monorepo functionality</name>
  <files></files>
  <action>
Run verification checks in order:

1. **TypeScript check on shared package:**

   ```bash
   cd packages/shared && bun run --bun tsc --noEmit && cd ../..
   ```

   Should complete without errors.

2. **TypeScript check on relay package:**

   ```bash
   cd packages/relay && bun run --bun tsc --noEmit && cd ../..
   ```

   Should complete without errors (relay imports from shared).

3. **Build the web package:**

   ```bash
   cd packages/web && bun run build && cd ../..
   ```

   Should produce .svelte-kit/cloudflare/\_worker.js.

4. **Verify dev script starts both processes (smoke test):**
   Start the dev processes in background, wait 5 seconds, check they are running, then kill them:

   ```bash
   timeout 5 bun run dev 2>&1 || true
   # The timeout kills after 5s; we just verify it starts without errors
   ```

   OR check the scripts parse correctly without errors:

   ```bash
   bun run --dry-run dev 2>&1 | head -20
   ```

5. **Verify wrangler deploy would work (dry-run):**
   ```bash
   cd packages/web && bunx wrangler deploy --dry-run 2>&1 | head -10 && cd ../..
   ```
   Should show deployment target without errors.

Clean up any generated files from tests.
</action>
<verify>
<automated>cd packages/shared && bun tsc --noEmit --noEmit && cd ../.. && echo "SHARED_TSC_OK" && cd packages/relay && bun tsc --noEmit --noEmit && cd ../.. && echo "RELAY_TSC_OK" && cd packages/web && bun run build > /dev/null 2>&1 && echo "WEB_BUILD_OK" && test -f .svelte-kit/cloudflare/\_worker.js</automated>
</verify>
<done>Shared types build clean, relay builds clean, web builds to .svelte-kit/cloudflare/\_worker.js, dev script parses without errors</done>
</task>

</tasks>

<verification>
- `bun run --cwd packages/web check` passes (type checking)
- `bun run --cwd packages/relay check` passes
- `bun run build` from root (or packages/web) produces Cloudflare Workers output
- `bun run dev` from root starts both web dev server and relay process
- `wrangler deploy` from packages/web would succeed
</verification>

<success_criteria>
`bun dev` from root starts both Vite dev server and relay process. `wrangler deploy` from packages/web deploys successfully. All three packages build without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-monorepo-restructure/05-03-SUMMARY.md`
</output>
