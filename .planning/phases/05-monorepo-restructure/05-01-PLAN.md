---
phase: 05-monorepo-restructure
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/web/.gitignore
  - packages/web/README.md
  - packages/web/components.json
  - packages/web/package.json
  - packages/web/src/app.css
  - packages/web/src/app.d.ts
  - packages/web/src/app.html
  - packages/web/src/lib/
  - packages/web/src/routes/
  - packages/web/static/
  - packages/web/svelte.config.js
  - packages/web/vite.config.ts
  - packages/web/tsconfig.json
  - packages/web/wrangler.jsonc
  - packages/web/worker-configuration.d.ts
  - packages/web/.prettierrc
  - packages/web/.prettierignore
  - packages/web/.vscode/
autonomous: true
requirements:
  - MONO-01
  - MONO-04
user_setup: []

must_haves:
  truths:
    - 'SvelteKit app runs from /packages/web with all existing functionality intact'
    - 'wrangler deploy from /packages/web deploys to Cloudflare Workers correctly'
  artifacts:
    - path: packages/web/
      provides: Restructured SvelteKit app with all source files moved intact
      min_lines: 50
    - path: packages/web/package.json
      provides: Web package with correct name (@river-stream/web) and all scripts/dependencies
      exports: dev, build, preview, check, push-stream, setup-signing
    - path: packages/web/wrangler.jsonc
      provides: Wrangler config with updated paths relative to new location
      contains: main = ".svelte-kit/cloudflare/_worker.js"
  key_links:
    - from: packages/web/vite.config.ts
      to: packages/shared
      via: devDependency on @river-stream/shared
      pattern: @river-stream/shared
    - from: packages/web/wrangler.jsonc
      to: packages/web/.svelte-kit/cloudflare/
      via: main entry pointing to compiled worker
      pattern: "\.svelte-kit/cloudflare/_worker\.js"
---

<objective>
Create `packages/web` — move the existing SvelteKit app into the monorepo workspace intact, update paths so it deploys correctly from its new location.
</objective>

<context>
@/Users/jspn/Documents/Sites/river-stream/05-CONTEXT.md
@/Users/jspn/Documents/Sites/river-stream/package.json
@/Users/jspn/Documents/Sites/river-stream/svelte.config.js
@/Users/jspn/Documents/Sites/river-stream/wrangler.jsonc
@/Users/jspn/Documents/Sites/river-stream/tsconfig.json
@/Users/jspn/Documents/Sites/river-stream/vite.config.ts
@/Users/jspn/Documents/Sites/river-stream/.gitignore
</context>

<interfaces>
<!-- Key contracts downstream plans depend on -->
packages/shared:
  - Name: @river-stream/shared
  - Purpose: Shared TypeScript types for demand API response shapes
  - Exports: Will be defined in packages/shared/index.ts (planned in 05-02)

packages/relay:

- Name: @river-stream/relay
- Purpose: TypeScript relay service (moves push-stream.ts here)
- Entry: packages/relay/src/index.ts (planned in 05-02)
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/web directory structure and move SvelteKit files</name>
  <files>packages/web/</files>
  <action>
Use `git mv` (per STATE.md decision) to move the following from root to `packages/web/`:

```
src/           → packages/web/src/
static/        → packages/web/static/
scripts/       → packages/web/scripts/
svelte.config.js → packages/web/svelte.config.js
vite.config.ts  → packages/web/vite.config.ts
tsconfig.json   → packages/web/tsconfig.json
wrangler.jsonc  → packages/web/wrangler.jsonc
worker-configuration.d.ts → packages/web/worker-configuration.d.ts
components.json → packages/web/components.json
.prettierrc     → packages/web/.prettierrc
.prettierignore → packages/web/.prettierignore
.vscode/        → packages/web/.vscode/
README.md       → packages/web/README.md
```

Commands:

```bash
mkdir -p packages/web
git mv src static scripts svelte.config.js vite.config.ts tsconfig.json wrangler.jsonc worker-configuration.d.ts components.json .prettierrc .prettierignore .vscode README.md packages/web/
```

IMPORTANT: Do NOT move `push-stream.ts` — it stays in packages/relay (created in plan 05-02). The `scripts/setup-signing.ts` moves to packages/web/scripts/ as-is (it belongs to the web app).
</action>
<verify>
<automated>test -d packages/web/src && test -d packages/web/static && test -f packages/web/svelte.config.js && test -f packages/web/wrangler.jsonc && test -f packages/web/scripts/setup-signing.ts && echo "MOVE_OK"</automated>
</verify>
<done>All SvelteKit source files exist under packages/web/ with git history preserved</done>
</task>

<task type="auto">
  <name>Task 2: Configure packages/web/package.json with correct name and dependencies</name>
  <files>packages/web/package.json</files>
  <action>
Create packages/web/package.json with:

```json
{
	"name": "@river-stream/web",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "wrangler dev .svelte-kit/cloudflare/_worker.js --port 4173",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"lint": "prettier --check .",
		"format": "prettier --write .",
		"gen": "wrangler types",
		"push-stream": "bun run scripts/push-stream.ts",
		"setup-signing": "bun run scripts/setup-signing.ts"
	},
	"devDependencies": {
		"@lucide/svelte": "^0.577.0",
		"@river-stream/shared": "workspace:*",
		"@sveltejs/adapter-cloudflare": "^7.2.6",
		"@sveltejs/kit": "^2.50.2",
		"@sveltejs/vite-plugin-svelte": "^6.2.4",
		"@tailwindcss/vite": "^4.1.18",
		"@types/node": "^25.5.0",
		"bits-ui": "^2.16.3",
		"clsx": "^2.1.1",
		"prettier": "^3.8.1",
		"prettier-plugin-svelte": "^3.4.1",
		"prettier-plugin-tailwindcss": "^0.7.2",
		"svelte": "^5.51.0",
		"svelte-check": "^4.4.2",
		"tailwind-merge": "^3.5.0",
		"tailwind-variants": "^3.2.2",
		"tailwindcss": "^4.1.18",
		"typescript": "^5.9.3",
		"vaul-svelte": "^1.0.0-next.7",
		"vite": "^7.3.1",
		"vite-plugin-devtools-json": "^1.0.0",
		"wrangler": "^4.63.0"
	},
	"dependencies": {
		"@fontsource-variable/dm-sans": "^5.2.8",
		"@fontsource-variable/syne": "^5.2.7",
		"stripe": "^20.4.1",
		"vidstack": "^0.6.15"
	}
}
```

Copy ALL devDependencies and dependencies from the original package.json. Add `"@river-stream/shared": "workspace:*"` to devDependencies (shared package will be created in plan 05-02).
</action>
<verify>
<automated>node -e "const p = JSON.parse(require('fs').readFileSync('packages/web/package.json')); const ok = p.name === '@river-stream/web' && p.devDependencies['@river-stream/shared'] && p.scripts.dev === 'vite dev'; console.log(ok ? 'PKG_OK' : 'PKG_FAIL:' + JSON.stringify(p))"</automated>
</verify>
<done>packages/web/package.json exists with name @river-stream/web, workspace:\* shared dependency, and all original scripts/dependencies</done>
</task>

<task type="auto">
  <name>Task 3: Update wrangler.jsonc paths relative to new location</name>
  <files>packages/web/wrangler.jsonc</files>
  <action>
Update packages/web/wrangler.jsonc — all paths are already relative to the package root (same as before since wrangler.jsonc stays in packages/web/), but confirm:

```jsonc
{
	"$schema": "./node_modules/wrangler/config-schema.json",
	"name": "stream",
	"compatibility_date": "2026-03-17",
	"compatibility_flags": ["nodejs_als"],
	"main": ".svelte-kit/cloudflare/_worker.js",
	"assets": {
		"binding": "ASSETS",
		"directory": ".svelte-kit/cloudflare"
	},
	"workers_dev": true,
	"preview_urls": true,
	"observability": {
		"logs": {
			"enabled": true,
			"invocation_logs": false,
			"persist": false
		}
	}
}
```

The main and assets paths remain `.svelte-kit/cloudflare/_worker.js` and `.svelte-kit/cloudflare` — these are relative to the wrangler.jsonc location in packages/web, so they resolve correctly from there.

Also create `packages/web/.gitignore` if missing:

```
.svelte-kit/
node_modules/
.env
.env.*
!.env.example
*.local
```

</action>
  <verify>
<automated>node -e "const w = JSON.parse(require('fs').readFileSync('packages/web/wrangler.jsonc', 'utf8').replace(/^\/\/.*$/gm, '')); console.log(w.main === '.svelte-kit/cloudflare/_worker.js' && w.assets.directory === '.svelte-kit/cloudflare' ? 'WRANGLER_OK' : 'WRANGLER_FAIL')"</automated>
</verify>
  <done>wrangler.jsonc has correct relative paths for Cloudflare Workers deployment from packages/web/ context</done>
</task>

</tasks>

<verification>
- `packages/web/src/routes/` exists and contains all original routes
- `packages/web/svelte.config.js` unchanged (same adapter-cloudflare config)
- `packages/web/package.json` has name `@river-stream/web`
- Wrangler paths resolve from packages/web/ root
</verification>

<success_criteria>
SvelteKit app at packages/web/ builds with `bun run build` and runs with `bun run dev` from packages/web/ directory.
</success_criteria>

<output>
After completion, create `.planning/phases/05-monorepo-restructure/05-01-SUMMARY.md`
</output>
