---
phase: 02-serverless-media-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/stream.ts
  - scripts/push-stream.ts
  - package.json
  - .env.example
autonomous: true
requirements:
  - STRM-01
user_setup:
  - service: cloudflare-stream
    why: 'Live stream ingest via RTMP push'
    env_vars:
      - name: CF_STREAM_LIVE_INPUT_KEY
        source: 'Cloudflare Dashboard → Stream → Live Inputs → [your input] → RTMPS Key'
      - name: CF_STREAM_LIVE_INPUT_UID
        source: 'Cloudflare Dashboard → Stream → Live Inputs → [your input] → UID'
      - name: CF_STREAM_CUSTOMER_CODE
        source: 'Cloudflare Dashboard → Stream → [any video] → HLS URL → customer-{code} segment'

must_haves:
  truths:
    - 'The local ffmpeg-to-HLS script (scripts/stream.ts) no longer exists in the repository'
    - 'A replacement push script (scripts/push-stream.ts) exists that uses ffmpeg to push RTSP to Cloudflare Stream via RTMPS'
    - 'Running `bun run push-stream` launches the push script (not the old local HLS script)'
    - 'All three Cloudflare Stream env vars are documented in .env.example'
  artifacts:
    - path: 'scripts/push-stream.ts'
      provides: 'RTSP-to-Cloudflare-Stream push script via ffmpeg RTMPS'
      contains: 'rtmps://live.cloudflare.com'
    - path: '.env.example'
      provides: 'Environment variable documentation'
      contains: 'CF_STREAM_LIVE_INPUT_KEY'
  key_links:
    - from: 'scripts/push-stream.ts'
      to: 'rtmps://live.cloudflare.com:443/live/{CF_STREAM_LIVE_INPUT_KEY}'
      via: 'ffmpeg -f flv output argument'
      pattern: 'rtmps://live.cloudflare.com'
    - from: 'package.json'
      to: 'scripts/push-stream.ts'
      via: '"push-stream": "bun run scripts/push-stream.ts"'
      pattern: 'push-stream'
---

<objective>
Replace the local `scripts/stream.ts` ffmpeg-to-HLS script with a new push-to-Cloudflare-Stream script.

Purpose: Removes the local ffmpeg dependency from the SvelteKit app's static directory, enabling Cloudflare deployment. The ingest now happens via an external push script that targets Cloudflare Stream's RTMPS ingest endpoint.

Output:

- `scripts/stream.ts` deleted
- `scripts/push-stream.ts` created (ffmpeg RTSP → RTMPS push)
- `package.json` script entry updated from `stream` to `push-stream`
- `.env.example` updated with three Cloudflare Stream env vars
  </objective>

<execution_context>
@/Users/jspn/Documents/Sites/river-stream/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jspn/Documents/Sites/river-stream/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-serverless-media-streaming/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create push-stream.ts and delete stream.ts</name>
  <files>scripts/push-stream.ts, scripts/stream.ts</files>
  <read_first>
    - scripts/stream.ts — the script being deleted; understand its structure to mirror in the new script
    - .env.example — current env vars documented, to understand what exists already
  </read_first>
  <action>
    1. DELETE `scripts/stream.ts` entirely (no backup, no fallback needed).

    2. CREATE `scripts/push-stream.ts` with the following implementation:

    ```typescript
    const rtspUrl = process.env.CAMERA_RTSP_URL;
    const liveInputKey = process.env.CF_STREAM_LIVE_INPUT_KEY;

    if (!rtspUrl) {
      console.error('Missing CAMERA_RTSP_URL in your environment.');
      process.exit(1);
    }

    if (!liveInputKey) {
      console.error('Missing CF_STREAM_LIVE_INPUT_KEY in your environment.');
      process.exit(1);
    }

    const rtmpsUrl = `rtmps://live.cloudflare.com:443/live/${liveInputKey}`;

    console.log(`Pushing RTSP stream to Cloudflare Stream...`);
    console.log(`Source: ${rtspUrl}`);
    console.log(`Destination: ${rtmpsUrl}`);

    const ffmpeg = Bun.spawn(
      [
        'ffmpeg',
        '-rtsp_transport', 'tcp',
        '-i', rtspUrl,
        '-c:v', 'copy',
        '-c:a', 'aac',
        '-f', 'flv',
        rtmpsUrl,
      ],
      {
        stdio: ['inherit', 'inherit', 'inherit'],
      }
    );

    process.exit(await ffmpeg.exited);
    ```

    Key differences from old script:
    - Output is `-f flv {rtmpsUrl}` (RTMPS push to Cloudflare) instead of `-f hls {outputFile}` (local HLS write)
    - Audio codec is `aac` (required by Cloudflare Stream; old script used `copy`)
    - No `mkdir` call needed (no local filesystem output)
    - New env var `CF_STREAM_LIVE_INPUT_KEY` required alongside existing `CAMERA_RTSP_URL`

  </action>
  <verify>
    <automated>test ! -f scripts/stream.ts && echo "stream.ts deleted" && grep -q "rtmps://live.cloudflare.com" scripts/push-stream.ts && echo "push-stream.ts has correct RTMPS URL"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/stream.ts` does NOT exist on disk
    - `scripts/push-stream.ts` exists on disk
    - `scripts/push-stream.ts` contains the string `rtmps://live.cloudflare.com:443/live/`
    - `scripts/push-stream.ts` contains `CF_STREAM_LIVE_INPUT_KEY`
    - `scripts/push-stream.ts` contains `-f`, `flv` as separate ffmpeg args (not `-f hls`)
    - `scripts/push-stream.ts` contains `aac` as audio codec (not `copy`)
  </acceptance_criteria>
  <done>Old local-HLS script deleted; new RTMPS push script exists with correct ffmpeg invocation targeting Cloudflare Stream ingest endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json and .env.example</name>
  <files>package.json, .env.example</files>
  <read_first>
    - package.json — find the `"stream": "bun run scripts/stream.ts"` entry to replace
    - .env.example — current env var list to append Cloudflare vars to
  </read_first>
  <action>
    1. In `package.json`, replace the `"stream"` script entry:
       - REMOVE: `"stream": "bun run scripts/stream.ts"`
       - ADD: `"push-stream": "bun run scripts/push-stream.ts"`

    2. In `.env.example`, append these three lines after the existing `CAMERA_RTSP_URL` line:
       ```
       CF_STREAM_LIVE_INPUT_KEY=your-live-input-rtmps-key
       CF_STREAM_LIVE_INPUT_UID=your-live-input-uid
       CF_STREAM_CUSTOMER_CODE=customer-abc123
       ```
       The full `.env.example` after update should be:
       ```
       CAMERA_RTSP_URL="rtsp://USERNAME:PASSWORD@10.0.0.87:554/h264Preview_01_sub"
       CF_STREAM_LIVE_INPUT_KEY=your-live-input-rtmps-key
       CF_STREAM_LIVE_INPUT_UID=your-live-input-uid
       CF_STREAM_CUSTOMER_CODE=customer-abc123
       STRIPE_SECRET_KEY=sk_test_...
       STRIPE_WEBHOOK_SECRET=whsec_...
       STRIPE_PRICE_ID=price_...
       COOKIE_SECRET=replace-me-for-local-dev
       ```

  </action>
  <verify>
    <automated>grep -q "push-stream" package.json && grep -q "CF_STREAM_LIVE_INPUT_KEY" .env.example && grep -q "CF_STREAM_LIVE_INPUT_UID" .env.example && grep -q "CF_STREAM_CUSTOMER_CODE" .env.example && ! grep -q '"stream":' package.json && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"push-stream": "bun run scripts/push-stream.ts"`
    - `package.json` does NOT contain `"stream": "bun run scripts/stream.ts"`
    - `.env.example` contains `CF_STREAM_LIVE_INPUT_KEY=`
    - `.env.example` contains `CF_STREAM_LIVE_INPUT_UID=`
    - `.env.example` contains `CF_STREAM_CUSTOMER_CODE=`
  </acceptance_criteria>
  <done>package.json has `push-stream` script pointing to new push script; .env.example documents all three Cloudflare Stream env vars.</done>
</task>

</tasks>

<verification>
After both tasks:
- `test ! -f scripts/stream.ts` exits 0
- `test -f scripts/push-stream.ts` exits 0
- `grep -q "rtmps://live.cloudflare.com" scripts/push-stream.ts` exits 0
- `grep -q "push-stream" package.json` exits 0
- `grep -q "CF_STREAM_LIVE_INPUT_KEY" .env.example` exits 0
- `bun run check` exits 0 (TypeScript check passes — stream.ts removal has no TS impact)
</verification>

<success_criteria>
STRM-01 satisfied: The local `ffmpeg` process (`scripts/stream.ts`) is removed. A replacement push script (`scripts/push-stream.ts`) exists that ingests RTSP and pushes to Cloudflare Stream via RTMPS. The new `bun run push-stream` command replaces `bun run stream`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-serverless-media-streaming/02-01-SUMMARY.md`
</output>
